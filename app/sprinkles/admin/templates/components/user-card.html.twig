{% extends "@account/components/user-card.html.twig" %}

{% block userCard_menu %}
    {% if checkAccess('uri_dashboard') %}
        <a href="{{site.uri.public}}/index" class="btn btn-default btn-flat btn-block">{{translate("ADMIN.PANEL")}}</a>
    {% endif %}
    {{ parent() }}
{% endblock %}
